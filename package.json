{
  "name": "ghostwriter-ai",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-auth-env.mjs && npm run compile:curriculum && tsc && vite build && npm run build:api",
    "preview": "vite preview",
    "setup:convex": "./scripts/setup-convex.sh",
    "check:health": "./scripts/check-health.sh",
    "test:webhook": "node ./scripts/test-webhook.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:unused": "tsc --noEmit -p tsconfig.lint.json",
    "build:api": "esbuild server/ai.ts --bundle --platform=node --format=esm --packages=external --outfile=api/ai.js && esbuild server/auth.ts --bundle --platform=node --format=esm --packages=external --outfile=api/auth.js && esbuild server/db.ts --bundle --platform=node --format=esm --packages=external --outfile=api/db.js && esbuild server/oauth-callback.ts --bundle --platform=node --format=esm --packages=external --outfile=api/oauth/callback.js && esbuild server/auth-apple.ts --bundle --platform=node --format=esm --packages=external --outfile=api/auth-apple.js",
    "compile:curriculum": "node scripts/compile-curriculum.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "@google/genai": "*",
    "@stripe/stripe-js": "^3.0.0",
    "buffer": "^6.0.3",
    "convex": "^1.18.0",
    "framer-motion": "^12.38.0",
    "jose": "^6.2.3",
    "openai": "^6.32.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "stripe": "^14.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vercel/node": "^3.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vitest": "^2.1.9"
  }
}
